---
title: 云服务器配置docker代理
tags: ["docker", "云服务器", "代理"]
date: 2026-03-22T15:28:09.27+0800
---

* 云服务器配置docker代理

** docker添加临时代理
#+begin_src bash
export HTTP_PROXY="http://127.0.0.1:7890"
export HTTPS_PROXY="http://127.0.0.1:7890"
docker pull ubuntu
#+end_src

** docker添加永久代理
#+begin_src bash
# docker添加永久代理
mkdir -p /etc/systemd/system/docker.service.d
cat - > /etc/systemd/system/docker.service.d/http-proxy.conf << 'EOF'
[Service]
Environment="HTTP_PROXY=http://127.0.0.1:10808"
Environment="HTTPS_PROXY=http://127.0.0.1:10808"
Environment="NO_PROXY=localhost,127.0.0.1,docker-registry.somecorporation.com"
EOF
systemctl daemon-reload
systemctl restart docker
#+end_src

*** 删除docker永久代理
#+begin_src bash
# 如果要删除添加的代理
rm -rf /etc/systemd/system/docker.service.d
systemctl daemon-reload
systemctl restart docker
#+end_src

** 参考链接
[[https://zhuanlan.zhihu.com/p/703839288][Docker 配置代理（临时/永久设置）]]

[[https://ynhugo.github.io/p/docker构建arm64与arm32交叉编译环境以及push与pull镜像/][Docker构建ARM64与ARM32交叉编译环境以及push与pull镜像]]
