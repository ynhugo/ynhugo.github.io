---
title: 怎样指定Docker存放image和容器的位置
tags: ["docker"]
date: 2026-03-23T02:34:48.51+0800
---

* 怎样指定Docker存放image和容器的位置

#+begin_src bash
# 终端打印修改结果，不会真正修改文件
cat /etc/docker/daemon.json | jq '."data-root" = "/data/disk/cache"'
# 或
jq '."data-root" = "/data/disk/cache"' /etc/docker/daemon.json
# 或
jq '.["data-root"] = "/data/disk/cache"' /etc/docker/daemon.json

# 验证修改结果后，直接修改文件
jq '.["data-root"] = "/data/disk/cache"' /etc/docker/daemon.json \
  > /etc/docker/daemon.json.tmp && \
mv /etc/docker/daemon.json.tmp /etc/docker/daemon.json

sudo systemctl daemon-reload
sudo systemctl restart docker
#+end_src

** 参考链接
[[https://zhuanlan.zhihu.com/p/13985193811][怎样指定 Docker存放image和容器的位置]]
