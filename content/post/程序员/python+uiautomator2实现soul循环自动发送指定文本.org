---
title: python+uiautomator2实现soul循环自动发送指定文本
tags: ["python", "uiautomator2", "soul"]
date: 2025-09-21T23:49:56.70+0800
---

* python+uiautomator2实现soul循环自动发送指定文本

** 环境要求

支持设备：安卓

手机要求：支持无线调试

运行环境：Termux

** Termux环境搭建

#+begin_src bash
pkg install -y libxml2 libxslt
pkg install libjpeg-turbo libtiff libwebp openjpeg
pip install uiautomator2
#+end_src

** Windows搭建环境

自行查找相关教程安装anaconda，版本最新即可，这里不过多赘述。

#+begin_src bash
# 创建一个名为uiautomator2的指定版本的环境，-y选项自动安装环境依赖
conda create -n uiautomator2 python=3.8 -y
# 激活环境
conda activate uiautomator2

pip install --pre uiautomator2 -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install keyboard -i https://pypi.tuna.tsinghua.edu.cn/simple
python -m uiautomator2 init		# 前提是USB调试打开的情况下已经连接无线调试或者USB连接电脑
git -c http.proxy="127.0.0.1:10808" clone https://github.com/openatx/weditor
pip install -e weditor -i https://pypi.tuna.tsinghua.edu.cn/simple
#+end_src

*** 参考链接

[[https://www.cnblogs.com/zzzBell/p/16130147.html][python安装weditor失败]]

** 功能

在soul派对界面发送指定文本到公屏。

** 代码

*** 版本1

#+begin_src python
# coding: utf-8
#
import os
import uiautomator2 as u2
import keyboard
import time

connect=os.system('adb devices | grep -w "device" | awk \'NR==1{print $1}\'')
d = u2.connect(connect)

while True:
    time.sleep(1);
    d(resourceId="cn.soulapp.android:id/tvChat").click()
    time.sleep(1);
    d.send_keys("❤️加入我们单身社交交友大家庭\n💛包脱单，包分配，包售后，包打假\n💚看群主瞬间小猪🐷猪蹄下边的水槽那一串黑黑的摩斯密码，备注soul名字，拉你进大家庭～", clear=True)
    time.sleep(1);
    d(resourceId="cn.soulapp.android:id/btnSend").click()
    d.click(0.402,0.745)
    time.sleep(60);
#+end_src

*** 版本2

**** 运行命令

#+begin_src python
conda activate uiautomator2
while true; do python soul自动发送.py; done
# 或
while true; do python soul自动发送.py device; done
#+end_src
device: 对应无线调试设备或USB设备	

**** 代码

#+begin_src python
# coding: utf-8
#
import os
import sys
import time
import subprocess
import keyboard
import uiautomator2 as u2

def get_device_id():
    # 获取第第一个已连接的Android设备ID
    try:
        result = subprocess.check_output(['adb', 'devices']).decode('utf-8')
        devices = [line.split('\t')[0]
            for line in result.splitlines()
            if '\tdevice' in line]
        return devices[0] if devices else None
    except (subprocess.CalledProcessError, IndexError):
        return None

def main():
    running = True
    
    # print(len(sys.argv))
    if len(sys.argv) > 1:        
        device_id = sys.argv[1]
        if not device_id:
            print("Error: No connect Android device found")
            sys.exit(1)

    else:
        device_id = get_device_id()
        if not device_id:
            print("Error: No connect Android device found")
            sys.exit(1)

    print(f"Connected device: {device_id}")

    # 初始化uiautomator2连接
    try:
        d = u2.connect(device_id)
        print("Device connected successfully")

        while running:
            d(resourceId="cn.soulapp.android:id/tvChat").click()
            time.sleep(1);        
            # d.send_keys("❤️加入我们单身社交交友大家庭\n💛包脱单，包分配，包售后，包打假\n💚看群主引力签，备注soul名字，拉你进大家庭～\n\n\n我们这个聚会，是包含吃火锅，可以喝酒，里边有KTV、台球、麻将，然后有社牛主持人带着大家做很多个破冰小游戏，互相了解，互相认识。就是你能够认识很多的朋友和异性，然后运气好的话，可以脱单儿。", clear=True)
            d.send_keys("❤️欢迎加入泡泡同城单身交友大家庭\n💛包脱单，包分配，包售后\n💚看管理瞬间小猪🐷猪屁股那一串白色的摩斯密码，备注soul名字，拉你进大家庭～\n\n\n今晚周六有个轰趴聚会，心动恋爱小游戏，火锅酒水一应俱全，小组破冰嗨起来，经典真心话大冒险，k歌桌游通通拿下。场地含KTV、台球、麻将，然后有社牛主持人带队，互相了解，互相认识。一次认识20➕同城单身朋友，欢迎加入～\n\n\n注意：不要只是关注群主，关注是没用的，公屏有提示", clear=True)
            # d.send_keys("test", clear=True)
            time.sleep(1);
            d(resourceId="cn.soulapp.android:id/btnSend").click()
            time.sleep(1);
            d.click(0.402,0.745)
            time.sleep(57);

    except Exception as e:
        print(f"Connected failed: {str(e)}")
        sys.exit(1)



if __name__ == "__main__":
    main()


#+end_src
