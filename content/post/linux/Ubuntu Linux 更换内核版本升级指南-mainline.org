---
title: "Ubuntu Linux 更换内核版本升级指南-mainline"
tags: ["linux", "ubuntu", "mainline"]
date: 2025-06-20T15:06:33.94+0800
---

* Ubuntu Linux 更换内核版本升级指南-mainline

** 快速切换任意版本的 Linux 内核

第一步，我们需要先安装 mainline 的软件包。只需要添加它的软件源，然后用 apt 安装即可：

#+begin_src bash
  sudo apt update
  sudo add-apt-repository ppa:cappelikan/ppa -y
  sudo apt update && sudo apt install mainline -y
#+end_src

安装好工具后，我们就可以指定想要安装的内核版本了。比如要安装 6.12.3 版本，可以直接运行：

#+begin_src bash
  sudo mainline install 6.12.3
#+end_src

等待安装完成后，只需要重启系统即可：

#+begin_src bash
  sudo reboot
#+end_src

使用 uname -r 命令直接查看内核版本号：

#+begin_src bash
  uname -r
#+end_src

** 参考链接

[[https://soulteary.com/2025/02/06/ubuntu-linux-kernel-upgrade-guide-with-mainline.html][Ubuntu Linux 内核版本升级指南：mainline]]

[[https://www.sysgeek.cn/ubuntu-install-linux-mainline-kernel/][如何在 Ubuntu 中安装 Linux Mainline 主线内核]]
